apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: my-ip-space
      protocol: layer2
      addresses: # use the command "minikube ip" to verify the ip address
      - 192.168.99.200 - 192.168.99.300